# AI Task Orchestrator 测试文档

AI Task Orchestrator 是一个基于 Spring Boot 的 AI 任务编排系统。

它的核心能力包括：

1. 创建 AI 任务
2. 查询任务状态
3. 记录任务事件
4. 支持异步任务执行
5. 后续可以扩展 RAG、Agent、Worker 调度和 KV Cache-aware Scheduling

这个文档用于测试 RAG 文档上传接口。